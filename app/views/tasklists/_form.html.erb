<%# locals: (task:, f:) %>

<div class="mt-4 mb-3"
     data-controller="task-checklist"
     data-task-checklist-action-plan-id-value="<%= task.bucket.action_plan.id %>"
     data-task-checklist-bucket-id-value="<%= task.bucket.id %>"
     data-task-checklist-task-id-value="<%= task.id %>">
  <div class="d-flex justify-content-between align-items-center mb-2">
    <div class="d-flex align-items-center gap-1">
      <h6 class="mb-0">Lista de verificação</h6>
      <span class="task-checklist-help"
            title="Você pode copiar as células da sua planilha aqui; cada célula vira um item."
            aria-label="Você pode copiar as células da sua planilha aqui; cada célula vira um item."
            tabindex="0">
        <i class="bi bi-question-circle" aria-hidden="true"></i>
      </span>
    </div>
    <div>
      <%= f.check_box :clone_tasklist_on_recurrence,
        class: "visually-hidden tasklist-clone-toggle" %>
      <%= f.label :clone_tasklist_on_recurrence,
        class: "icon-btn task-checklist-action tasklist-clone-toggle-button",
        data: {
          task_checklist_target: "cloneToggleButton"
        },
        title: "Clonar lista de verificação ao repetir",
        aria: { label: "Clonar lista de verificação ao repetir" } do %>
        <i class="bi bi-repeat" aria-hidden="true"></i>
      <% end %>
      <button type="button" class="icon-btn task-checklist-action" title="Adicionar item" aria-label="Adicionar item" data-action="click->task-checklist#addItem">
        <i class="bi bi-plus-lg" aria-hidden="true"></i>
      </button>
      <button type="button" class="icon-btn task-checklist-action danger" title="Limpar checklist" aria-label="Limpar checklist" data-action="click->task-checklist#clearAllItems">
        <i class="bi bi-trash3" aria-hidden="true"></i>
      </button>
    </div>
  </div>

  <div class="task-checklist-surface">
    <%= f.fields_for :tasklist, task.tasklist || task.build_tasklist do |tasklist_form| %>
      <%= tasklist_form.hidden_field :id %>
      <div data-task-checklist-target="itemsContainer"
           data-action="paste->task-checklist#pasteItems"
           id="tasklist-items-<%= task.id %>">
        <% task.tasklist&.tasklist_items&.each do |item| %>
          <%= render "tasklist_items/item", item: item, task: task %>
        <% end %>
      </div>

      <!-- Template mantido como fallback para adição local (caso a requisição falhe) -->
      <template data-task-checklist-target="itemTemplate">
        <div class="task-checklist-item mb-2 d-flex align-items-center gap-2">
          <input type="hidden" name="task[tasklist_attributes][tasklist_items_attributes][NEW_RECORD][id]" value="">
          <input type="hidden" name="task[tasklist_attributes][tasklist_items_attributes][NEW_RECORD][_destroy]" value="false">
          <input type="checkbox" name="task[tasklist_attributes][tasklist_items_attributes][NEW_RECORD][completed]" value="1" class="form-check-input">
          <input type="text" name="task[tasklist_attributes][tasklist_items_attributes][NEW_RECORD][content]" class="flex-grow-1 task-checklist-item__input" placeholder="Novo item" autocomplete="off">
          <button type="button" class="btn btn-sm btn-link text-danger" data-action="click->task-checklist#removeItem">🗑</button>
        </div>
      </template>
    <% end %>
  </div>
</div>
