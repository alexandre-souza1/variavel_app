<div class="container mt-4">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1>Novo Centro de Custo</h1>
    <%= link_to 'Voltar', admin_cost_centers_path, class: 'btn btn-secondary' %>
  </div>

  <div class="card">
    <div class="card-body">
      <%= render 'form', cost_center: @cost_center %>
    </div>
  </div>
</div>
